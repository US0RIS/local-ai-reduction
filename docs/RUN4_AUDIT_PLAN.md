# Run 4 audit closure plan

Run 4 addresses the second external audit of Run 3. No Run 3 headline value is promoted further until these checks are complete.

Priority corrections:

1. add value-basis pseudoinverse / inverse-Gram correction and account its bytes;
2. rerun the equal-compute control on the exact 100,032-character held-out stream used by corrected L2C;
3. evaluate teacher and shared student with the same stored Q4 weight representation used by byte accounting;
4. pin canonical Q4 scale derivation and signed-extremum behavior with boundary golden vectors;
5. regenerate reproducible benchmark artifacts from current source and introduce manifest/hash checking;
6. redesign native projected-operator fidelity benchmark with a nearly exact rank-32 source;
7. correct basis scale-byte accounting and restore context sweeps;
8. identify the old d=64 conformance context/dtype provenance.

Longer experimental work:

- convergence/equal-compute learning curves with teacher-at-320 control;
- lower-metadata latent value quantization;
- real-checkpoint activation spectra;
- end-to-end measured RSS/VRAM.
